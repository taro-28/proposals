# ローコードSaaSのUXを向上させるためのTypeScript

はじめまして、今日は「ローコードSaaSのUXを向上させるためのTypeScript」というタイトルで発表をさせていただきます。
よろしくお願いいたします。

## 自己紹介

まず自己紹介ですが大西太郎と申します。
ハンドルネームもtaroでこちらの水色のアライグマのアイコンでTwitterなどをやっています。
仕事は株式会社ベースマキナでソフトウェアエンジニアとして働いていて、ローコードで管理画面を作成できるベースマキナというサービスを作っています。
こんな感じにAPIやDBの接続情報と、リクエストの情報やSQLなどを登録すると簡単にデータの登録フォームや一覧テーブルなどが作成できるサービスです。
今回はそのベースマキナでの開発で実際に行った事例のお話をさせていただきます。

またベースマキナでは、TSKaigi Kansaiのゴールドスポンサーをさせていただいておりブースがございます。
よかったらぜひお立ち寄りください。

それでは本題に入っていきます。

## 導入

今回はタイトルにもある通り、ローコードSaaSのベースマキナでUXを向上させるためにTypeScriptの機能を活用している話をします。

### ローコードSaaSのUXを向上させるためのTypeScriptとは？

トークの内容を紹介する前に、まずタイトルの「UXを向上させるためのTypeScript」という部分を簡単に説明します。

ベースマキナはローコードで管理画面を作成するサービスのため、一部ユーザーがJavaScriptで設定を書く機能があります。
例えば簡単な機能の例がこちらです。
これはSQLの結果をJavaScriptで少し加工してテーブルに表示しています。
またもう少し複雑な機能の例だとこんな感じです。

これはデータベースにユーザーのレコードを追加した後に、作成されたidを使ってストレージにプロフィール画像をアップロードする場合の設定です。
設定後にこちらのフォームからメールアドレスとファイルを選択して実行すると、JavaScriptで設定した処理が実行されます。

最初の例程度であれば簡単に入力ができますが、後者のような例の場合はこのexecuteActionのような専用の関数が用意されているとはいえ一定のコード量があり、

値の型がわかるかどうか、
入力補完が効くか、
また誤ったコードを書いてしまった場合のバリデーションが効くか、
そのエラーメッセージがわかりやすいか

などがUXの良し悪しに関わってきます。

今日はこういったユーザーにJavaScriptを設定してもらう機能のUXを向上させるために、
どんなTypeScriptの機能や関連ライブラリを活用しているかをお話しできたらと思っています。

### トークの流れ

ということで今日のトークの流れです。

今日は大きく2つの話をさせていただきます。

1つ目は先ほどお見せしたようなブラウザ上のコードエディターでの編集体験を向上させる話です。
2つ目は設定したJavaScriptのコードを実行した時のバリデーションの話です。

基本的にはTypeScriptの話ですが、話の都合上少しだけMonaco EditorやZodなどのライブラリが登場する部分があります。
ただ話の内容に関わる部分は都度簡単な解説を入れるのと、ほぼほぼTypeScriptの話ですのでご安心いただければと思います。

また各トピックは、「ここで補完が効くようにしたい」みたいなやりたいことに対して、具体的にどうしたのか？という流れで話していきます。
ぜひ最初の「やりたいこと」のところでみなさんならどうするかを考えてみていただけたら嬉しいです。
もし僕らと違う方法やもっとよくできる点などがあったら、ぜひ懇親会やブースで教えていただけると嬉しいです。

## ローコードツールのJavaScriptのコードエディターをTypeScriptの機能で便利にする

では早速1つ目のエディターの話に入っていきます。

まず最初にユーザーがどんなJavaScriptのコードを書く機能なのかと
エディターで使用しているMonaco Editorというライブラリについて簡単に紹介し、
その後TypeScriptの機能を使ってユーザー体験を向上させた3つの話をします。

### どんなJavaScriptのコードを設定する機能なのか？

ということで、簡単に機能の紹介です。

今日話すのは先ほどお見せしたデータベースやストレージを操作する処理をJavaScriptで設定できる機能です。

<!-- TODO アクションとは何かを伝える -->

このexecuteActionというところで、事前に作成したSQLでDBにユーザーを追加する処理や、ストレージにファイルをアップロードする処理を呼び出しています。

ユーザーが書くJavaScriptの形式は、ECMAScript modulesの形式で、default exportする関数の中に処理を書いていきます。
この関数の第1引数にはこのJavaScriptを実行する際に画面のフォームで入力した値が渡ってきて、
その値はJavaScriptを設定する際にこのようなフォームで一緒に設定できます。
また1行目のように@basemachina/actionというpackageからベースマキナ側で用意した組み込みの関数などをimportして使用することもできます。

では次にこのエディターを作成するために使用しているMonaco Editorというライブラリの紹介です。

### Monaco Editor

<!-- TODO 調べる -->

Monaco Editorは、Visual Studio Codeのエディター部分をWebアプリケーション用に抽出したJavaScriptベースのコードエディターです。
高度なコード補完、構文ハイライト、エラーチェック、シンタックスチェックなどの機能を提供し、Web上での開発者向けのインターフェースとして広く使われています。

### エディターのUXを向上させる

ではTypeScriptを使ってエディターのユーザー体験を向上させるためにやった話に入っていきます。

### default exportされる関数の引数にフォームの状態から動的に生成した型をつける

まず1つ目はこのdefault exportされる関数の引数に型をつける話です。

#### やりたいこと

具体的にはこの第1引数のargsに型をつけて、ホバーで型情報を確認できるようにしたり補完を効かせたりするのがやりたいことです。
ここでargsの型はこちらのフォームの設定内容に応じて変わります。
例えば、nameという名前でテキストのパラメーターを作った場合はキーがname、値がstringのオブジェクト型となり、さらにageという名前で数値のパラメーターを追加した場合はキーがage値がnumber | nullのプロパティが追加され、こんな感じに型が効くようにするのがゴールです。
ちなみにパラメーターの種類は、テキスト、数値、日付、真偽値、配列、タプル、ファイルなどがあります。
さてこのJavaScriptの関数に対してどんな方法で型をつけるのが良いでしょうか？

#### やったこと

僕らはこんな感じでJSDocの型注釈を使うことにしました。
普段TypeScriptで開発をしている方なら、このようなJSDocを見たことある人も多いのではないでしょうか？
あのよくフレームワークやライブラリのconfigファイルなど使われている方法です。
たとえばNext.jsのjsでのconfigファイルの例を見てみるとこんな感じでJSDocの型注釈が使われています。

普段TypeScriptを使っていると使うことはほとんどないかもしれませんが、JSDocではtypeタグってのを使うとTypeScriptと同様に型を指定できます。
またtypedefタグを使うと型定義もできて、それをtypeタグで設定することもできます。
さらに冒頭のJSDocのように別のpackageからimportしてきた型を指定することもできます。

ここで@basemachina/actionからの型のexportは通常のpackageと同様に型定義ファイルを作成してexportするだけです。
そのためHandler型は、型定義ファイル内でこんな感じに関数型を定義してexportするだけで、importしてJSDocのtypeタグで型を指定することができます。

ちなみにMonaco EditorではaddExtraLibsというメソッドを使って、エディター内で使用できるpackageを追加できます。

さてJavaScriptの関数にTypeScriptで型をつける方法がわかったので、次はフォームの入力値から動的に型を生成する部分です。
ここはそんなにおしゃれな技はなく泥臭く文字列の結合で、@basemachina/actionの型宣言とその中のHandler型を生成しています。

ベースマキナではフォームの状態管理にformikを使っていて、
formikはフォームの状態をReactのstateで管理して制御コンポーネントのinputを使用するライブラリで
フォームのstateをこんな感じにhookなどで取得できるため、
これをuseEffectで監視してフォームの値が変化するたびに、
都度型定義ファイルのコードを生成してMonaco Editorに同期しています。

最終的にはこんな感じにフォームの設定内容に応じて動的に変化する型を第1引数につけることができました。

### 組み込み（のpackageからimportする）関数をつける

では次は組み込み関数に型をつける方法です。

#### やりたいこと

組み込み関数ってのはこの@basemachina/actionからimportできる関数のことです。
この関数に型をつけて、型情報や補完で関数の使い方をわかりやすくするのがやりたいことです。
さっきの話を踏まえるとこれはほぼ自明かなと思います。

#### やったこと

そうですね。
Handler型と同様に、@basemachina/actionの型宣言の中にexecuteActionなどの型を定義してexportするだけです。

ただここで問題があって、それがなにかというと、

このexecuteActionという関数の引数の型の部分で、
この第1引数がstringと第2引数がRecord<string, any>となっているのですが、
実際はユーザーが作成したアクションのデータに基づいて渡せる値に制限があります。

どういうことかというと、
まず第1引数にはアクションのID/識別子が入ります。
IDってのはアクションを作成した時点で振られるUUIDで、識別子はIDと別でユーザーが任意に設定できるユニークな値です。
そのため型的にはstringでありつつも、存在しないID/識別子を渡してしまうと実行時にエラーになってしまいます。

次に第2引数は呼び出すアクションに渡すパラメータで、キーがパラメーターの名前、値がパラメーターの値となるオブジェクトです。
例えばこのような設定のアクションを呼び出す場合は、第1引数にこtのIDor`create-user`という識別子、第2引数に`{ name: 'taro', age: 20 }`のようなオブジェクトを渡すことができます。
そのため型的にはRecord<string, any>なんですが、パラメーター設定と一致しない値を渡してしまうと実行時にエラーになってしまいます。

この問題をユーザーが作成したアクションのデータを元にいい感じに型をつけて、解決するのが最後の話です。

### ユーザーが作成したデータを元に型をつける

では改めて先ほどの問題をまとめるとこんな感じです。

#### やりたいこと

これに対してやりたいことは以下の2つです。

まず第1引数のアクションのID or 識別子の型は、ユーザーが作成したすべてのアクションのID/識別子のユニオン型にして、こんな感じに補完が効くようにしたいです。
次に第2引数のパラメータの型は、第1引数で指定したアクションに設定されているパラメーターの型を値に持つオブジェクトにしたいです。
具体的にはこんな感じに`create-user`と入力すると、`{ name: string, age: number }`のような型が推論され、nと打つとnameが補完されるようにしたいです。

さてこれはexecuteActionをどうするのがよいでしょうか？

#### やったこと

まず結論ですがこんな型定義にしました。
ここでActionIdはすべてのアクションのID or 識別子がユニオンになっている型です。
ActionArgumentsMapはアクションのID or 識別子がキーになっていて、値がそのアクションのパラメーターのオブジェクト型のオブジェクト型です。
見ての通り、ActionIdが型引数になっているので第1引数の値を入力すると、第2引数の方が絞り込まれるようになっています。

その他詳細は省略しますが、実際は以下のような細かい要件に対応させたりしてもう少し複雑な型定義になっています。

<!-- - 必須かどうか
- 初期値があるかどうか
- そもそもパラメーターがない場合もある -->

実際にこのexecuteActionの型を書き換えてみると、こんな感じに推論が効くようになります。

あとは最後にこの型定義をDBに保存されているデータから生成するだけです。
ベースマキナではバックエンドサーバーからのデータ取得にGraphQL、データ取得兼状態管理ライブラリにはApollo Clientを使っています。
Apollo ClientはGraqphQLでよく使われるバックエンドから取得したデータをReactのstateに詰めていい感じにキャッシュしてくれるライブラリで、基本的にはswrやTanStack Queryなどと同じようなライブラリです。

Apollo Clientではこんな感じにhookを使って取得したデータのstateにアクセスできて、あとはフォームのstateの時と同様にuseEffectで監視してゴリゴリ型定義のコードを生成するだけです。

最終的にはこんな感じに自分が作成したアクションのデータに基づいて、バチバチに推論が効くようになります。
こういった自分が作成したデータに基づいて動的に型が変わる編集体験はローコードサービス特有のもので、個人的には結構面白いポイントだなと思っています。

### エディターの話のまとめ

ではエディターの話のまとめです。
この後まだバリデーションの話があります！時間半分で終わる気ではないでご安心ください笑

以下の2つを知っていただけたら嬉しいです。

- JSDocのtypeタグを使うとTypeScriptのようにJavaScriptに型をつけることができる
- ローコードサービスでは、フォームやDBの値から動的にゴリゴリ型定義を生成してエディターの補完や型情報を向上させている

ということで次の話に進むのですが、ちょっと小休憩がてらエディターの話の小ネタの盛り合わせです。

## 次の話に行くまでの小休憩で前の話でハマったポイントの紹介

まずMonaco EditorでJavaScriptのコードに型を効かせるためには、JSDocや型定義ファイルの生成だけでなくtsconfigなどの設定も必要です。
実際に設定しているtsconfigはこちらです。
ちなみにallowJsがtrueじゃないとそもそも型は聞きません。
またstrictNullChecksが実はハマりポイントで、これがないとnullが型で表示されないです。

またMonaco Editorの設定もいくつかあります。
特にはハマったのがquickSuggestionsの設定です。
これはVSCodeにもあるのですが、これがないとユニオン型の補完が効かないです。

Monaco Editorを触っていると、こういった普段はなんとなく動いているTypeScriptまわりのツールの設定を1つ1つ調べていかないといけないのが大変でもありつつ面白いポイントです。

もう1つJSDocの話ですが、実際はHandlerの型定義意外にも色んな部分でJSDocを活用しています。
例えば組み込み関数のJSDocはこんな感じです。
関数の説明やドキュメントのURL、パラメータの説明やデフォルト値などの色んな情報をJSDocに書いておくことで、出来るだけユーザー自身が調べなくてもコードが書けるようにしています。

さて2分ほどの小休憩でしたが、次はバリデーションの話に入っていきます。

<!-- fireStoreアクションの話を入れてもいいかも -->

<!-- 全体的に話の流れをもっと整理する -->

## 組み込みの関数の引数の型をzodのスキーマで定義して、実行前に引数の値をバリデートし、日本語のエラーメッセージを表示する

では以下の流れで組み込み関数のバリデーションの話をしていきます。

まずそもそもどんなバリデーションをなぜやりたいのかっていうところを紹介し、
その後、前提となるJavaScriptのコードとその中の組み込み関数が実行される流れを簡単に紹介し、
その後以下の3stepでバリデーション機構を作りながらどんなTypeScriptの機能や関連ライブラリを活用しているのかを紹介していきます。

### 組み込み関数のバリデーションとは？

バリデーションといってなんのバリデーションかというと、先ほどまで話して組み込み関数の実行時のバリデーションの話です。
先ほどまで組み込み関数に型をつける話をしていたのですが、型はついていてもあくまで実行するのはJavaScriptなので、組み込み関数の引数は型と異なる値を渡して実行ってのはできてしまいます。
そのため組み込み関数の実行時に関数の引数の値を型と一致しているかを検証して、型と合わない場合はこんな感じに実行時にエラーメッセージを返せるようなバリデーションを作りたいという話です。

### JavaScriptのコードと組み込み関数が実行される流れ

ではバリデーション機構の話に入る前に、そもそも組み込み関数含めユーザーが書いたJavaScriptのコードがどのように実行されているかの簡単な紹介をします。
登場するのは以下の3つです。

- まずユーザーが操作するベースマキナのWebのフロントエンド
- ベースマキナのメインのバックエンドサーバー
- 最後がJavaScriptのコードを実行するためのNode.jsのサーバー

です。

1. 画面からパラメーターの値を入力すると、webからapiにリクエストが飛び、apiは実行されたアクションに設定されているJavaScriptのコードを取得して、Node.jsのサーバーにパラメーターの値と一緒に送信します。
2. Node.jsのサーバーはJavaScriptのコードとパラメーターの値を受け取り、組み込み関数を提供する@basemachina/actionのモジュール解決をして、argsにパラメーターの値を渡してexport defaultされた関数を実行します。
3. 実行後関数の戻り値をapiに返し、apiがwebに返して終了です。

この実行環境の話も面白いのですが、TypeScriptではなくJavaScriptの話になってしまうので省力します。
よかったら懇親会やブースでお話ししましょう。

ここでモジュール解決するための組み込み関数はこんな感じにTypeScriptで実装して、解決用のオブジェクトに関数を登録しています。

ではここからが本題のバリデーションの話です。

改めてやりたいことは組み込みの関数の実行前に、関数の引数が型通りの値かどうかをバリデーションしてエラーメッセージを返す機構を作ることです。

さてどうするのが良いと思いますか？

僕らが実際にやったのは、組み込み関数とその引数をバリデーションするためのzodのスキーマを受け取り、組み込み関数の実行前にバリデーションを差し込むような関数を作る方法です。

この機構の詳細を話す前に、知っている人も多いかもしれませんがzodとは何かを簡単に説明します。

zodはTypeScriptのバリデーションライブラリです。
こんな感じにスキーマを書いて、値をバリデーションできます。
TypeScriptを使ったことのある方であれば、1度はフォームのバリデーションなどで使ったことがあるのではないでしょうか？
zodの便利な点はスキーマからバリデーション後の値の型を推論できることです。

今回作成した機構でもこのスキーマからの型推論を活用していて、スキーマを渡すとこんな感じに関数の引数の型が推論され、普通にTypeScript関数を書くのと同様に組み込み関数を実装できます。

ではこの機構の詳細を見ていきます。
generateBuiltInFuncの中身はこんな感じです。
ポイントは以下の2つです。

- 組み込み関数の引数をzodのスキーマから推論された型で片付けしている。
- 組み込み関数の引数の値として渡された値を、実際の組み込み関数に渡す前にバリデーションを入れている。

基本的な機構としてはとてもシンプルです。
これで実行するとこんな感じにバリデーションが効いて、エラーメッセージが表示されます。
見ての通りzodのエラーメッセージがそのまま返ってしまっていて、日本向けに提供しているサービスのエラーメッセージとしてはあまりにユーザーフレンドリーではありませんが、これの対応は後述します。

さてこの機構を使って実装する組み込み関数で、こんな型の関数を実装したい場合はどうすれば良いでしょうか？
第1引数の値の型によって第2引数の型が変わるいわゆるオーバーロードというやつです。

これはどのようにzodのスキーマを渡すのが良いと思いますか？

僕らが実際にやったのはz.tuple([z.string(), z.number()])とz.tuple([z.number(), z.string()])のように、複数のスキーマを渡せるようにしました。
組み込み関数の型は、こんな感じに複数の関数の型のユニオン型になっていて、処理の中で条件分岐を書くとちゃんとオーバーロードができていることがわかります。

スキーマからz.inferで推論した型を組み込み関数の引数に当てる部分はこんな感じになっています。
だいぶ複雑になりましたが肝は、スキーマの配列を複数の関数のウニオン型に変換するためのMapped Typesです。

Mapped Typesというのは、ユニオン型を使ってこのように書くとオブジェクトのキーの値に型をつけられる機能です。
今回はこのMapped Typesを応用して、キーがスキーマの配列のindex、値が各スキーマに対応する組み込み関数のオブジェクトを作成し、そのオブジェクトの[number]を取って関数のユニオン型に変換しています。

バリデーション部分の変更はそんなに複雑ではなく、スキーマを上から順番に適用して全て失敗した場合のみ最初のスキーマのエラーメッセージを返すようにしています。

さてだいぶ柔軟に引数の型をスキーマで定義できるようになってきましたが、この場合はどうでしょうか？
複数のスキーマを渡す際に、スキーマの長さが異なる場合です。

この場合型推論部分は特に変更がありません。
ただ問題になるのはバリデーションの部分でいつどのスキーマのエラーメッセージを返すべきかです。
例えば長さが2と3のスキーマを渡して、3番目の引数の値が合わなかった場合に最初の長さ2のスキーマのエラーメッセージを返してしまうと、あたかも第3引数が渡せないようなエラーメッセージが返ってしまいます。

そのためだいぶ泥臭い実装になってきましたが、引数の長さと同じ長さのスキーマを最初に適用するようにしています。

さてこれで機構の大部分は完成で、現状の僕らの組み込み関数の引数の型はすべてスキーマで表現できるようになり、普通にTypeScriptの関数を書くのと同様に実装しながら実行前にバリデーションができるようになりました。

<!-- 日本語化の話、ts関係ないから消すかおまけでサクッとくらいでいいかも -->

ただこのままだとエラーメッセージがあまりにもわかりづらく、とてもUXを向上させるための話には適しません。
ということでエラーメッセージをわかりやすくしていきます。

まずはエラーメッセージの日本語化です。
今回のむずかしいのはスキーマに対して任意の値が渡され発生するエラーも色々あり、各関数ごとにエラーメッセージを設定していくのはとても大変な点です。
そのためベースマキナではzodのエラーメッセージを各言語に翻訳できる、zod-i18n-mapというaijiさん作のライブラリを使用しています。
これを導入するとこんな感じにエラーメッセージが非本語で変えるようになります。
さらにエラーメッセージの表現もこんな感じに簡単に上書きできます。

あとはエラーの発生位置がわからない問題です。

これはzodのエラーメッセージをいい感じに整形していきます。
zodでは発生したエラーがスキーマのどの位置で発生したがわかるようになっているので、それを使ってエラーメッセージを整形します。
ここは特におしゃれな方法はなく、zodのエラーの構造に合わせて泥臭く成形しています。
実際に実行するとこんな感じにわかりやすいエラーメッセージが表示されるようになりました。

これで完成です！
まとめに入る前に最後にちょっとだけおまけです。

<!-- 消すかも -->

### おまけ

zodのスキーマごとにエラーメッセージを定義するのが大変なので楽をしているという話をしましたが、続いて社内で毎回スキーマを書くのが面倒という話もでてきました。
例えば、こういった組み込み関数の内部でほぼGraphQLのoperationを呼んでいるだけの関数です。
この対応のために、graphqlスキーマからzodのスキーマを自動生成できるgraphql-codegen-validation-schemaを使用しています。
こんな感じにzodのスキーマが生成されるのでそれをimportするだけになりとても楽になりました。

またTypeScriptというかJavaScriptのおかげの話ですが、この機構はブラウザでもサーバーでも共通で使用できます。
今回紹介したJavaScriptアクションはユーザーが書いたJavaScriptコードをサーバーで実行する機能ですが、ベースマキナにはユーザーがJSXを書いて管理画面を作成できるビューという機能があります。
このビューでも同様に組み込みの関数（ただの関数、hook、コンポーネント）があり、やりたいことは同じです。

<!-- 発表まで機構をビューに導入する -->

これはmonorepoで、バリデーション機構をpackageにしてそれぞれのworkspaceでimportすることで共通で使えています。

ここまででバリデーションの話も終わりです。

## 最後に

では最後にまとめです。

ベースマキナではユーザーがJavaScriptで設定を書く機能があるため、エディターやバリデーションなどでTypeScriptをUXのために活用する例がいろいろあります。
個人的には普段自分がエディターや外部ライブラリの利用者側で使用するようなTypeScriptの機能を、作る側になるのがむずかしくもあり面白いなと感じています。
今回の発表でTypeScriptを使う仕事の1つとして、ローコードサービスの開発も面白いよ！というのが伝われば幸いです。

以上です。ありがとうございました。
このあとはブースにいるので、ぜひ色んなTypeScriptのお話をしましょう！

<!-- 時間調整用 -->
<!-- ## 今後技術的にやれたら良さそうなこと

- ベースマキナの今後のロードマップとは関係がありません
- taroが個人的にやれたら技術的に楽しそうだと思っていることです

### 外部ライブラリが使えるようになった場合の型周りの対応

- 任意または固定のnpm packageが使えるようになった場合です。
- エディターの対応
  - もし固定の外部ライブラリの場合、その外部ライブラリの型定義ファイルをimportして文字列に変換し、型定義ファイルとしてaddExtraLibする
  - 任意の外部ライブラリの場合、esm.shでリアリタイムに型定義ファイルを取得して、それをaddExtraLibする（godaiさんの記事が参考になります）
- バリデーション機構の対応
  - 難しそう
  - 型からzodのスキーマを生成する必要がある…？ -->

<!-- ### 組み込み関数の型定義を自動生成する

- graphqlのqueryの結果に依存してたりするからむずかしい -->

## TODO

- viewの関数もzodでバリデーションする
- @basemachina/actionの型定義ファイルのMonacoへの同期でuseEffectを使うのをやめる
- declare moduleの書き方が正しいか見直す
- できるだけTypeScriptの機能名を入れたい
